# 게임 개발자 성장 커리큘럼 (Unity 우선 + 코테 병행)

Gemini에서 진행하던 스터디를 Claude Code로 이전한 문서.
기간 제한 없음. 진도 기준으로만 움직인다. "다음"이라고 하면 다음 학습 유닛을 제시받는다.

---

## 0. 운영 방식 (책사 프로토콜)

- 아부 없이 데이터와 코드 형상에 기반한 판단만 말한다. 잘못된 코드는 잘못됐다고 말한다.
- 전체 방향은 이 문서가 담당하고, "다음"이라고 하면 현재 위치 기준 다음 유닛(목표, 실습, 주의점)을 구체적으로 제시한다.
- 실습 게임을 만들 때는 "무엇을 신경 써야 하는지"를 먼저 제시하고, 완성 후 코드 리뷰를 요청하면 그 관점으로 리뷰한다.
- 이미 아는 부분은 "가볍게 하고 다음"이라고 말하면 건너뛴다. 수학 기초는 있으나 잊은 상태이므로, 필요한 시점에 필요한 만큼만 복습한다.
- 접근 원칙: 엔진 종속 기능보다 **엔진 간 공통 원리**(수학, 자료구조, 렌더링 파이프라인, 게임 루프)를 우선한다. 이것이 나중에 언리얼 전환 비용을 낮춘다.

## 1. 전체 구조: 4개 트랙

```
트랙 A (메인)   유니티 심화 ──────────────────────> 언리얼 전환
트랙 B (병행)   코딩테스트 C++ (매일 조금씩, 끝까지 병행)
트랙 C (중반 삽입)  서비스 기술: 비동기, API, Go 서버, SDK, IAP
트랙 D (수시 삽입)  연출/아트 파이프라인: Timeline, 쉐이더, Spine, 레벨디자인
```

### 우선순위와 그 근거 (날카로운 진실)

1. **유니티 심화가 최우선.** 요구 목록(SDK, Go, IAP, 쉐이더, Spine...)은 전부 "게임이 이미 있는 사람"의 기술이다. 붙일 게임 없이 SDK 연동부터 배우면 남는 게 없다. 트랙 C는 트랙 A의 Phase 5 이후에 시작한다.
2. **코테는 지금 당장, 매일.** 몰아서 하는 코테 공부는 실패한다. 하루 1~2문제를 트랙 A와 무관하게 병행한다. 언어는 C++ 고정.
3. **Spine은 후순위.** 2D 프로젝트가 없으면 배워도 증발한다. 2D 실습 프로젝트(Phase 7)에 도달했을 때 붙인다.
4. **언리얼은 유니티 중급 완성(Phase 8) 후.** 두 엔진을 동시에 초급으로 배우는 것이 최악의 수다. 유니티에서 공통 원리를 잡고, 언리얼은 "매핑"으로 배운다.

---

## 2. 트랙 A — 유니티 메인 라인

### 완료: Phase 1~2 (Gemini에서 진행, `study/Assets/GeminiAIStudy`)

- **Phase 1 (탑다운 슈팅 기초)**: Transform 이동, `Input.GetAxis`, 벡터 정규화, 프리팹 Instantiate/Destroy, 코루틴 스포너, 태그 검색, 충돌, 간이 싱글톤 GameManager, TMP 점수 UI, `Time.timeScale` 게임오버, 씬 재시작, 사운드
- **Phase 2 (3D 캐릭터 + 데이터)**: CharacterController 이동/점프/중력, Animator 파라미터 연동, ScriptableObject 아이템(`ItemData`), List 인벤토리, Raycast 상호작용(E키 줍기), 인벤토리 UI 동적 슬롯
- **별도 완료: A\* 길찾기** (`study/Assets/Astar`): 그리드 생성, 장애물 감지, A* 직접 구현, 클릭 이동. 미완 과제: 2D 확장, openSet 힙 최적화

### Phase 3 — 전투 시스템과 적 FSM (현재 위치, 다음 시작점)

목표: "살아있는 게임"의 최소 단위인 전투 루프 완성.
- 체력 시스템: `IDamageable` 인터페이스로 플레이어/적 공통 처리 (인터페이스 첫 실전 사용)
- 적 상태 머신: enum + switch로 대기-추적-공격-사망 FSM 구현
- 체력바 UI (월드 스페이스 캔버스), 피격 피드백(무적시간, 넉백)
- NavMesh 기초: 직접 만든 A*와 NavMeshAgent 비교 — 언제 뭘 쓰는지 판단 기준 세우기
- Phase 2 인벤토리 연결: HealthPotion 사용 시 회복

실습 시 신경 쓸 것: 데미지 계산 로직을 MonoBehaviour에서 분리할 수 있는지 고민해볼 것. 코드 리뷰 관점: FSM 상태 전이 조건이 한 곳에 모여 있는가, Update에 로직이 뭉쳐 있지 않은가.

### Phase 4 — 데이터 처리와 구조화

목표: 요구사항 "데이터 처리 및 구조화"의 본체.
- 저장/불러오기: `JsonUtility` + `Application.persistentDataPath`, PlayerPrefs와의 용도 구분
- ScriptableObject 아키텍처: 아이템 DB, 적 스탯 테이블을 SO로 설계
- C# 이벤트/델리게이트: 인벤토리 변경 시 UI 자동 갱신을 이벤트 기반으로 리팩터링 (현재 InventoryUI는 열 때마다 전체 재생성 — 이걸 교보재로 사용)
- 직렬화의 함정: SO 참조를 JSON으로 저장할 때 생기는 문제와 해법(ID 매핑)

코드 리뷰 관점: 데이터(SO)와 상태(런타임 인스턴스)가 분리되어 있는가. SO 원본을 런타임에 수정하는 사고가 없는가.

### Phase 5 — 구조 설계와 코드 품질

목표: 요구사항 "구조 설계". 지금까지 만든 코드를 재료로 리팩터링.
- 신 Input System 이전 (프로젝트에 `InputSystem_Actions.inputactions` 이미 존재, 코드만 구식)
- 오브젝트 풀링: Phase 1 총알/적의 Instantiate/Destroy 교체, GC 스파이크 프로파일러로 확인
- 제대로 된 싱글톤 vs 대안(직렬화 참조, 이벤트 채널), `FindObjectOfType` 계열의 비용
- 레이어 구분 연습: 입력 → 로직 → 표현(애니메이션/사운드/UI)을 분리하는 컴포넌트 설계
- Unity 프로젝트에서의 어셈블리 정의(asmdef) 맛보기

### Phase 6 — 물리 엔진과 3D 수학 (요구사항 "물리 엔진, 복잡한 3D 연산")

- 수학 복습(잊은 것 되살리기): 내적/외적의 게임 활용(시야각 판정, 좌우 판별), 쿼터니언 vs 오일러각, 행렬과 좌표계 변환(local/world)
- Rigidbody 제대로: `FixedUpdate`와 물리 스텝, 힘/속도/토크, 보간 설정
- 이동 방식 3종 비교 정리: Transform vs CharacterController vs Rigidbody — 선택 기준 문서화
- 충돌 해석: OnCollision vs OnTrigger, 레이어 충돌 매트릭스, `Physics.Raycast` 계열 총정리
- 실습: 물리 기반 미니게임 (예: 포탄 궤적 예측선 그리기 — 포물선 수식 직접 계산)

이 Phase가 언리얼 전환 시 가장 크게 재활용되는 구간이다. 엔진이 바뀌어도 수학은 안 바뀐다.

### Phase 7 — 2D 프로젝트 + Spine + A* 마무리

- 2D 기초: SpriteRenderer, Physics2D, Tilemap
- A* 미완 과제 해소: 2D 버전 구현 + openSet 우선순위 큐(힙) 교체 — 트랙 B(코테)에서 배운 힙을 실전 적용하는 접점
- **Spine**: 2D 스켈레탈 애니메이션 파이프라인 — 에디터에서 리깅/애니메이션, Unity 런타임 연동, 스킨/트랙 개념
- 실습: 2D 횡스크롤 또는 탑다운 미니게임에 Spine 캐릭터 얹기

### Phase 8 — 연출 파이프라인과 쉐이더 (요구사항 "전투 연출용 툴", "쉐이더")

- **Timeline**: 컷신/스킬 연출 — 애니메이션 트랙, 시그널, 커스텀 트랙 개념
- Cinemachine: 카메라 연출(추적, 흔들림, 블렌딩)
- 파티클 시스템 기초 + VFX 그래프 맛보기
- **쉐이더**: 렌더링 파이프라인 개념(버텍스→프래그먼트) → Shader Graph로 시작(디졸브, 아웃라인, 피격 플래시) → HLSL 코드 쉐이더 한 번은 직접 작성
- 실습: Phase 3 전투에 스킬 연출 1개 완성 (Timeline + 파티클 + 카메라 + 쉐이더 총동원)

### Phase 9 — 레벨디자인과 완성 (요구사항 "레벨디자인")

- 레벨디자인 이론 기초: 동선 유도, 페이싱, 인카운터 설계 (이론은 가볍게, 실습 중심)
- ProBuilder로 그레이박싱, 라이팅 기초(라이트맵, 라이트 프로브)
- **졸업 과제: 초소형 게임 1개 완성** — 타이틀→게임→결과 씬 흐름, 저장, 사운드, 연출 포함. PC 빌드 후 itch.io 업로드
- 완성 후 전체 코드 리뷰 요청 (아키텍처 관점)

### Phase 10 — 언리얼 전환 (트랙 D 합류점)

- 유니티 개념 → 언리얼 매핑표로 시작: GameObject/Component ↔ Actor/Component, 프리팹 ↔ 블루프린트 클래스, C# ↔ C++/블루프린트
- 코테로 다진 C++이 여기서 회수된다 (우선순위 근거 중 하나)
- 이 Phase의 세부 커리큘럼은 도달 시점에 다시 설계한다. 미리 짜두면 그때의 실력과 안 맞는다.

---

## 3. 트랙 B — 코딩테스트 (C++, 매일 병행)

시작 시점: **지금 당장.** 트랙 A와 독립적으로 하루 1~2문제.
플랫폼: 백준(단계별) + 프로그래머스(레벨별). 아래 순서로.

1. **입출력과 구현**: 빠른 입출력(`ios::sync_with_stdio(false)`), 문자열 처리, 시뮬레이션 문제
2. **STL 손에 익히기**: `vector`, `string`, `map`/`unordered_map`, `set`, `priority_queue`, `sort` + 커스텀 비교자 — 게임 코드에서도 그대로 쓰는 도구들
3. **시간복잡도 감각**: 입력 크기 보고 O(n²)이 되는지 즉시 판단하는 훈련
4. **완전탐색, 정렬, 이분탐색**
5. **BFS/DFS**: 이미 A*를 구현해봤으므로 진입이 빠를 것. 그래프 순회 → 최단거리 BFS → 백트래킹
6. **DP**: 가장 오래 걸리는 구간. 점화식 세우는 연습 위주로
7. **그리디, 투포인터, 누적합**
8. **그래프 심화**: 다익스트라(A*의 friend), 유니온파인드, 위상정렬

공부 방식: 30분 고민 → 못 풀면 풀이를 보되 **다음날 안 보고 다시 구현**. 이 재구현 단계를 생략하면 실력이 늘지 않는다. 틀린 문제는 리스트로 관리.

## 4. 트랙 C — 서비스 기술 (Phase 5 이후 시작)

붙일 게임이 생긴 뒤에 시작한다. 순서 자체가 커리큘럼이다.

1. **C# 비동기**: async/await, Task, 코루틴과의 차이, 메인스레드 제약 — 이후 전부의 전제
2. **REST API 소비**: `UnityWebRequest`로 공개 API 호출, JSON 파싱 — "API를 이용"의 시작점
3. **Go 서버 기초**: Go 문법 속성 → `net/http`로 점수 저장/랭킹 API 서버 직접 제작 → 내 게임과 연결. DB는 SQLite로 시작
4. **3rd-Party SDK 연동**: Firebase(인증 + Analytics)를 첫 SDK로 — 연동 과정에서 만나는 문제(플랫폼별 설정, 버전 충돌)가 진짜 학습 내용
5. **In-App 결제**: Unity IAP — 상품 등록, 구매 플로우, 영수증 검증(서버 검증을 3번의 Go 서버로 직접 구현하면 최고의 실습)

주의: 4~5번은 모바일 빌드가 전제이므로 Android 빌드 경험을 먼저 만든다.

## 5. 공부 방식 권장

- **만들면서 배운다**: 각 Phase는 실습 결과물이 완성 조건. 강의/문서만 보는 진도는 진도로 치지 않는다.
- **코드 리뷰 사이클**: 실습 완성 → 리뷰 요청 → 지적사항 반영 → 다음 Phase. 리뷰 없이 넘어가면 나쁜 습관이 굳는다.
- **커밋 습관**: 학습 유닛 단위로 커밋 (현재 커밋 스타일 유지). 리뷰도 커밋 단위로 하면 쉽다.
- **문서화**: 각 Phase 폴더에 ReadMe.md로 배운 것 요약 (Astar/ReadMe.md처럼 — 이미 하고 있는 좋은 습관)
- **막히면 30분 룰**: 30분 이상 막히면 질문. 삽질도 학습이지만 30분 넘는 삽질은 비용이다.

## 6. 진행 기록

| 날짜 | 한 일 |
|---|---|
| ~2026-07 | Gemini와 Phase 1(슈팅 기초), Phase 2(3D 캐릭터, 인벤토리), A* 구현 |
| 2026-07-10 | Claude Code로 이전, GitHub 연동, 전체 커리큘럼 수립. 다음: 트랙 A Phase 3 + 트랙 B 시작 |
